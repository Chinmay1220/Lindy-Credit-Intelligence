dbt_lindy:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "lindy_mock"
      schema: "main"
      schemas_and_paths:
        main: "../data/lindy_mock.db"
      schema_directory: "../data"